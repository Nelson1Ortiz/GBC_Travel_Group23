@model GBC_Travel_Group23.Models.CarRental

@{
    ViewData["Title"] = "Car Rental Details";
    string? mode = ViewBag.Mode; // Assuming you have a similar mechanism for Car Rentals
}

<h2>@ViewData["Title"]</h2>

<div class="container mt-4">
    <div class="row">
        <div class="col">
            <div class="card">
                <div class="card-header bg-dark text-white">
                    @if (mode == "edit")
                    {
                        <span>Edit Car Rental</span>
                    }
                    else
                    {
                        <span>Car Rental Details</span>
                    }
                </div>
                <div class="card-body">
                    @if (mode != "edit")
                    {
                        <!-- Display Car Rental Details -->
                        <dl class="row">
                            <!-- Fix: Display fields for Car Rental -->
                        </dl>
                        <div class="text-center">
                            <!-- Fix: Add/Edit/Delete buttons -->
                        </div>
                    }
                    else
                    {
                        <!-- Fix: Edit form fields for Car Rental -->
                    }
                </div>
            </div>
        </div>
    </div>
</div>
