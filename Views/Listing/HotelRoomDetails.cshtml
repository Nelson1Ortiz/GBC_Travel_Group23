@model GBC_Travel_Group23.Models.HotelRoom

@{
    ViewData["Title"] = "Hotel Room Details";
    string? mode = ViewBag.Mode; // Assuming you have a similar mechanism for Hotel Rooms
}

<h2>@ViewData["Title"]</h2>

<div class="container mt-4">
    <div class="row">
        <div class="col">
            <div class="card">
                <div class="card-header bg-dark text-white">
                    @if (mode == "edit")
                    {
                        <span>Edit Hotel Room</span>
                    }
                    else
                    {
                        <span>Hotel Room Details</span>
                    }
                </div>
                <div class="card-body">
                    @if (mode != "edit")
                    {
                        <!-- Display Hotel Room Details -->
                        <dl class="row">
                            <!-- Fix: Display fields for Hotel Room -->
                        </dl>
                        <div class="text-center">
                            <!-- Fix: Add/Edit/Delete buttons -->
                        </div>
                    }
                    else
                    {
                        <!-- Fix: Edit form fields for Hotel Room -->
                    }
                </div>
            </div>
        </div>
    </div>
</div>
